{
  "name": "sunway_pyramid_app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel ./*.html",
    "build": "parcel build ./*.html && mv dist .tmp_dist && mv .tmp_dist dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.7.0"
  }
}

